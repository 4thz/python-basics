number = int(input("Введите целое положительное число: "))
maximum = number % 10
number = number // 10
while number // 10 > 10:
    check = number % 10
    if check > maximum:
        maximum = check
    number = number // 10
print("Максимальная цифра во введённом числе: ", maximum)
